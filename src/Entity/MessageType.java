package Entity;

/**
 * Enum class used to represent the different message types used in the program.
 */
public enum MessageType {
    message,
    logIn,
    logOut,
    registerUser,
    loginFail,
    loginSuccess,
    registerFail,
    registerSuccess,
    addFriends,
    userLoggedIn,
    userLoggedOut
}
